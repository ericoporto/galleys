// new module header

import String DirToString(CharacterDirection dir);

import int MaxI(int a, int b);
import int MinI(int a, int b);

/// Clamps v between min and max, inclusive.
import int ClampI(int v, int min, int max);